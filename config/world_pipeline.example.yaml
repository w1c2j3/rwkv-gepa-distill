defaults:
  api_protocol: chat_completions
  timeout_seconds: 120
  num_retries: 3
  max_tokens: 512
  prefer_stream: false

base_models:
  from_env_collection: ANSWER_MODEL

variant_generator_model:
  from_env_prefix: GENERATOR_MODEL

prompt_optimizer_model:
  from_env_prefix: OPTIMIZER_MODEL
