paths:
  val_seeds: data/seeds/val.jsonl
  train_seeds: data/seeds/train.jsonl
  best_prompt: artifacts/best_prompt.txt
  optimization_report: artifacts/optimization_report.json
  distill_output: data/distill/distill_small.jsonl

optimization:
  max_metric_calls: 12
  fallback_mode: heuristic_search
